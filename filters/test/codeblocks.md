wuppie fluppie

```java
class Foo {}
```

foo bar

``` {.haskell size="tiny"}
class Monad m where
    (>>=) :: m a -> (a -> m b) -> m b
    return :: a -> m a
```

lorem ipsum

`void` is to be expected.

`void`{.c}: same but highlighting.
